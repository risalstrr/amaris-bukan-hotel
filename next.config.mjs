/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pelitariau.com', 'media.istockphoto.com', 'cdn.prod.website-files.com', 'img.freepik.com', '5.imimg.com'],
    },
};

export default nextConfig;